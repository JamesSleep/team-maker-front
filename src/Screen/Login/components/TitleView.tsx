import React from 'react';
import { LoginStyles } from '../styles/login.styles';

const { TitleView, Title } = LoginStyles;

export default () => (
  <TitleView>
    <Title>로그인</Title>
  </TitleView>
);
